<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="table-001" author="naav">
            <sql>
                CREATE TABLE bookingsystem.venue(
                id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                name VARCHAR(100) NOT NULL,
                address VARCHAR(255) NOT NULL,
                totalcapacity BIGINT NOT NULL
                );
            </sql>

    </changeSet>
    <changeSet id="table-002" author="naav">
        <sql>
            CREATE TABLE bookingsystem.event(
            id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            name VARCHAR(255) NOT NULL,
            totalcapacity BIGINT NOT NULL,
            leftcapacity BIGINT NOT NULL,
            venueid BIGINT NOT NULL ,
            CONSTRAINT fk_event_venue FOREIGN KEY (venueid) REFERENCES bookingsystem.venue(id) ON DELETE CASCADE
            );
        </sql>
    </changeSet>

</databaseChangeLog>
