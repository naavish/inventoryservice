<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="table-001" author="naav">
            <sql>
                CREATE TABLE bookingsystem.venue(
                id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                name VARCHAR(100) NOT NULL,
                address VARCHAR(255) NOT NULL,
                totalcapacity BIGINT NOT NULL
                );
            </sql>

    </changeSet>
    <changeSet id="table-002" author="naav">
        <sql>
            CREATE TABLE bookingsystem.event(
            id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            name VARCHAR(255) NOT NULL,
            totalcapacity BIGINT NOT NULL,
            leftcapacity BIGINT NOT NULL,
            venueid BIGINT NOT NULL ,
            CONSTRAINT fk_event_venue FOREIGN KEY (venueid) REFERENCES bookingsystem.venue(id) ON DELETE CASCADE
            );
        </sql>
    </changeSet>

    <changeSet id="table-003" author="naav">
        <sql>
            ALTER TABLE bookingsystem.event
            ADD COLUMN ticketprice DECIMAL(10,2) NOT NULL DEFAULT 10.00;
        </sql>
    </changeSet>

    <changeSet id="table-004" author="naav">
        <sql>
            CREATE TABLE bookingsystem.customer(
            id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            name VARCHAR(255) NOT NULL,
            email VARCHAR(255) NOT NULL,
            address VARCHAR(255) NOT NULL
            );
        </sql>
    </changeSet>
    <changeSet id="table-005" author="naav">
        <sql>
            CREATE TABLE bookingsystem.orders(
                id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                total DECIMAL (10,2) NOT NULL,
                quantity BIGINT NOT NULL,
                placed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                customer_id BIGINT ,
                event_id BIGINT ,
                CONSTRAINT fk_order_customer FOREIGN KEY (customer_id) REFERENCES bookingsystem.customer(id) ON DELETE SET NULL,
                CONSTRAINT fk_order_event FOREIGN KEY (event_id) REFERENCES bookingsystem.event(id) ON DELETE SET NULL
                );
        </sql>
    </changeSet>


</databaseChangeLog>
